function multiplicar(num1: number, num2: number): number {
    return num1 * num2;
}

function saudacao(nome: string): string {
    return "Olá " + nome
}

console.log(multiplicar(5, 10));
console.log(saudacao("Lukas, Bem vindo ao TypeScript!"));